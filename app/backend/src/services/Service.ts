// import { ILoginModel } from '../interfaces/ILogin';

// export default class LoginService {
//   private loginModel: ILoginModel;

//   constructor(model: ILoginModel) {
//     this.loginModel = model;
//   }

//   async login(user) {
//     const result = await this.loginModel.login(user);
//   }
// }
